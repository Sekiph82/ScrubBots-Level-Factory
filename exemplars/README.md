# Production exemplar inbox

The production WFC exemplar registry is intentionally empty in M05. This directory is an owner-controlled inbox; no owner-supplied artwork is shipped here, and files are not loaded by the runtime. Only a future owner-approved review process may populate it.

The synthetic logical exemplars used by tests live under `tests/fixtures/wfc/` and are not production assets.
