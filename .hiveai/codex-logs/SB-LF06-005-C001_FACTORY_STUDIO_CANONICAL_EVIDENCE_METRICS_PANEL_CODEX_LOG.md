# SB-LF06-005-C001 — Factory Studio Canonical Evidence / Metrics Panel
Document role: CODEX BUILDER LOG

## Start

- Starting timestamp: 2026-09-16 Europe/Istanbul.
- Scope: `SB-LF06-005-C001` only.
- Canonical repository: `https://github.com/Sekiph82/ScrubBots-Level-Factory`.
- Canonical local mirror: `C:\Users\sekip\Desktop\Scrubbots - Pixel Art Generator`.
- Branch: `main`.
- The mirror was synchronized non-destructively with `git fetch origin main` and `git merge --ff-only origin/main`; it advanced from `bb2acb4a721ea79229d87cb7d6aae5b9ca417fba` to `8888e32`.
- Starting HEAD after synchronization: `8888e32`.
- `origin/main` after synchronization: `8888e32`; local branch was equal to origin.
- Initial status: clean except for the five pre-existing untracked Godot UID files under `level_factory/scripts/`; those owner-local files were preserved.
- Stashes and the single canonical worktree were inspected. No sibling repository was used.

## Required records and contracts read before edits

- Root `TASKS.md`, including the active `SB-LF06-005-C001` frontier and parser/status contract.
- `AGENTS.md` and `GOVERNANCE.md`.
- Closing strict audit: `.hiveai/audits/SB-LF06-004-C001_FACTORY_STUDIO_CRISP_CANONICAL_ARTWORK_PREVIEW_STRICT_AUDIT.md`.
- Authoritative prompt: `.hiveai/prompts/SB-LF06-005-C001_FACTORY_STUDIO_CANONICAL_EVIDENCE_METRICS_PANEL_PROMPT.md`.
- Accepted Factory Studio shell, workspace, target controls, canonical action bridge, crisp artwork preview, runtime suite, action integration suite, and focused LF06-001..004 tests.
- Canonical evidence contracts: `src/scrubbots_pixel_factory/output/bundle.py`, `src/scrubbots_pixel_factory/quality/core.py`, `src/scrubbots_pixel_factory/core/request.py`, and `src/scrubbots_pixel_factory/core/result.py`.
- Current `level_factory/README.md`, directory boundaries, and clean-checkout tests.

This log was created and verified before any implementation, test, documentation, or governance edit for LF06-005. Root `TASKS.md`, prior audits, prior prompts, and prior logs remain untouched.

## Initial command evidence

- `git remote -v`, branch/HEAD/origin/status, stash list, and worktree inspection: repository identity and `main` mirror verified.
- `git fetch origin main; git merge --ff-only origin/main`: fast-forward synchronization succeeded.
- Read-only contract inspection completed before this log was created.

## Implementation

Implementation notes will be appended chronologically after the read-only canonical evidence panel is selected and verified.

## Implementation and correction history

- Added `level_factory/scripts/factory_studio_evidence_panel.gd` as a dedicated read-only presentation component. It reads only `<successful action output_path>/metadata.json`, performs narrow schema/identity/path checks, exposes EMPTY/READY/ERROR snapshots, and keeps solution, measured difficulty, and load/risk explicitly unavailable.
- The panel displays canonical identity/provenance, request difficulty only as target/request context, the real quality binding/report schema and policy version, selected structural metrics copied from canonical metadata, rejection codes, and the explicit `Structural QA ACCEPT != OWNER ACCEPT` distinction. It does not recompute quality, create a solver, or persist a second evidence store.
- Wired the panel into the existing target-controls action-result handoff through the repository-compatible dynamic script boundary. The canonical action bridge, crisp preview, last-success action evidence, and preview state remain separate and unchanged in semantics.
- Extended the committed Studio/Core integration suite to prove pre-success EMPTY state, real Generate metadata identity/metrics, semantic unavailable gates, failed-action retention, Reproduce MATCH metadata source switching, canonical identity consistency, and corrupt-success-metadata ERROR behavior while preserving prior evidence.
- Added `tests/unit/test_sb_lf06_005_factory_studio_evidence_panel.py` for canonical schema cross-checks, local/offline/source boundaries, semantic gates, no Core duplication, and the real integration runner.
- Extended the existing LF00 project-boundary GDScript allow-list only for the authorized dedicated evidence panel.

### Failed commands and corrections

- Initial focused LF06-005 run: two static assertions expected `path.join` and case-sensitive lowercase source markers. Corrected the tests to assert Godot's `path_join` and compare semantic markers case-insensitively; the runtime integration test was already passing.
- First combined LF06/LF01/LF00 regression run: five failures were caused by the ignored `level_factory/scripts/__pycache__` generated by an earlier compileall run being included by existing project-file scans, plus the new authorized evidence script not yet being listed in the existing LF00 implementation allow-list. Removed the exact generated cache directory and added only `factory_studio_evidence_panel.gd` to that allow-list. No owner files were deleted.
- Added a committed runtime mismatch branch after the initial green run: a valid reproduction metadata object is modified only in memory/test output to carry a mismatched artwork candidate ID, then the panel is required to enter ERROR while retaining prior evidence. The original metadata is restored before cleanup.

## Verification before implementation commit

- Focused LF06-005 suite: `4 passed`, with the known local pytest-cache permission warning.
- Final retained LF06-001/002/003/004/005, LF01-005, and LF00-001/002/006/007/008 focused regression set: `109 passed`, with the same known local pytest-cache permission warning.
- Full `python -m pytest -q`: `703 passed`, one known local pytest-cache permission warning.
- `python -m compileall -q src tests level_factory/scripts/factory_core_launcher.py`: passed. The exact generated `level_factory/scripts/__pycache__` was removed afterward. The committed integration runner cleaned its generated `level_factory/output` test tree; the output boundary contains only the tracked `.gitkeep` after the run.
- `godot --headless --path level_factory --quit`: passed with exit code 0.
- The committed Studio/Core integration runner passed through the focused LF06-005 test and exercised real Generate/Reproduce metadata, preview, retention, unsupported/corrupt metadata, and mismatched metadata behavior.
- `git diff --check`: passed.
- Scope review: only the LF06-005 evidence panel, existing target-controls handoff, committed Studio integration evidence, one narrow LF00 boundary allow-list update for the authorized panel, focused LF06-005 Python evidence, and this matching builder log are staged. Root `TASKS.md`, canonical Python Core semantics, providers, solver/difficulty implementation, Dashboard, Import, Library, Content Platform, main-game files, and SB-LF06-006+ files are untouched.

## Publication checkpoints

Implementation commit and push details will be appended after the implementation commit. The final log-only publication commit will be the terminal commit and will contain no product or test changes.
