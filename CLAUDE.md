# H!veAI TASKS-only control-plane adapter

Before doing project work, read root `TASKS.md` as the sole current
task-state authority and the authoritative prompt supplied in the handoff.
The tracked GitHub branch is current-state authority; local folders are
execution workspaces.

Do not claim independent acceptance or mutate files reserved to the independent auditor/tracker owner.

## H!veAI GitHub tracking

- The repository root `TASKS.md` is the only current project-management/task-state tracker.
- Prompts, builder logs, audits, and other documents are process/evidence archives, not tracker inputs.
- Do not create or revive legacy `.hiveai` tracker/control-plane files, cycle indexes, or a lowercase root `tasks.md`.
